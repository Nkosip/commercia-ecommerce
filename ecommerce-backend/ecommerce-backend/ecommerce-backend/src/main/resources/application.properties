spring.application.name=Ecommerce Backend
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db
spring.datasource.username=root
spring.datasource.password=
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create-drop

# ============================================================================
# ADD THESE TO YOUR application.properties FILE
# ============================================================================

# ============================================================================
# ADMIN USER INITIALIZATION CONFIGURATION
# ============================================================================
# These settings control the automatic creation of the admin user on startup

# Enable/disable automatic data initialization (set to false in production after first run)
app.init.enabled=true

# Admin user credentials

# ============================================================================
# SECURITY NOTES:
# ============================================================================
# 1. Change the default password immediately after first login!
# 2. In production, consider setting app.init.enabled=false after initial setup
# 3. For production, use environment variables instead of hardcoding:
#    - Set via: export APP_ADMIN_PASSWORD=YourSecurePassword
#    - Then use: app.admin.password=${APP_ADMIN_PASSWORD}
# ============================================================================

# ============================================================================
# PRODUCTION RECOMMENDATIONS:
# ============================================================================
# Option 1: Use environment variables
# app.admin.email=${ADMIN_EMAIL:admin@afrikatikkun.com}
# app.admin.password=${ADMIN_PASSWORD:Admin@2024}
#
# Option 2: Disable after first run
# app.init.enabled=false
#
# Option 3: Use Spring profiles
# Create application-prod.properties with:
# app.init.enabled=false
# ============================================================================


stripe.api.key=sk_test_51SzRw2PJIkwKQwVCrSDW2p5Mb4IVkfr8mpyTm9hyv3cYOX4BQsFTE94EeMh50gpe16YrdGqlJL9IhF3KApwc5vgj00SRuYPScn
stripe.secret.key=sk_test_51SzRw2PJIkwKQwVCrSDW2p5Mb4IVkfr8mpyTm9hyv3cYOX4BQsFTE94EeMh50gpe16YrdGqlJL9IhF3KApwc5vgj00SRuYPScn

stripe.currency=zar
stripe.webhook.secret=